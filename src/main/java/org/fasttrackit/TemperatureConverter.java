package org.fasttrackit;

public class TemperatureConverter {

    // A.
    // 1. Creati o metoda care sa primeasca o valoare numerica (temperatura)
    // si sa o transforme din Celsius in Fahrenheit
    // 2. Creati o metoda care sa primeasca o valoare numerica (temperatura)
    // si sa o transforme din Fahrenheit in Celsius
    // 3. Apelati fiecare metoda din main() cu diferite valori.
    // Metodele ar trebi sa afiseze valorile inainte si dupa conversie
    //B.
    // 1. Creati o singura metoda care sa primeasca temperatura (numeric)
    // si tipul de temperatura dorit (string) si sa realizeze conversia necesara
    // 2. Apelati metoda din main pentru fiecare caz
//    The formulas are
//            C = (F − 32) × 5 / 9
//    and
//            F = (C × 9 / 5) + 32
    public static void main(String[] args) {

        String a = "Celsius";
        if (a.equals("Celsius")) {
            System.out.println("conditie indeplinita");
        } else {

        }

    }


}
